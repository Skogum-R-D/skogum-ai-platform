apiVersion: apps/v1
kind: Deployment
metadata:
  name: researcher
  labels:
    app: researcher
spec:
  replicas: 1
  selector:
    matchLabels:
      app: researcher
  template:
    metadata:
      labels:
        app: researcher
    spec:
      containers:
        - name: researcher
          image: skogum/researcher:latest
          env:
            - name: VALKEY_URL
              value: "valkey://valkey:6379"
            - name: MISTRAL_API_KEY
              valueFrom:
                secretKeyRef:
                  name: mistral-secret
                  key: api-key
            - name: RESEARCHER_MODEL
              value: "mistral-medium-latest"
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github-secret
                  key: token
            - name: GH_ORG
              valueFrom:
                secretKeyRef:
                  name: github-secret
                  key: org
                  optional: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - python
                - -c
                - "import valkey; valkey.from_url('valkey://valkey:6379').ping()"
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
